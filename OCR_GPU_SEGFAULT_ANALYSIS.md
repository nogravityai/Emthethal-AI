# 🛑 توثيق العطل الفني الجذري في محرك PaddleOCR على مستوى الـ GPU

تم إعداد هذا التوثيق الهندسي الشامل لشرح الأسباب الفنية الدقيقة التي تمنع تشغيل مكتبة **`paddlepaddle-gpu==2.6.2`** داخل بيئة الكونتينر الحالية (`python:3.11-slim`)، والخطوات الجراحية التي تم تطبيقها لمحاولة تجاوز العطل مع الحفاظ التام على قيود المشروع.

---

## 🔬 1. ملخص المشكلة (Executive Summary)
عند محاولة تشغيل الاستنتاج (Inference) لصور المستندات المعقدة عبر مكتبة PaddleOCR المسرعة بالـ GPU، ينهار البرنامج الفرعي فوراً مصحوباً برسالة الخطأ القاتلة من نظام التشغيل:
```text
FatalError: `Segmentation fault` is detected by the operating system.
SignalInfo: *** SIGSEGV (@0x0) received by PID from PID 0 ***
```
يحدث هذا الانهيار تحديداً داخل مكتبة C++ المترجمة في دالة التهيئة الأساسية للمحرك:
`paddle_infer::Predictor::Predictor` و `paddle::AnalysisPredictor::Init`.

---

## 🛠️ 2. التحليل المعماري والحلول المطبقة (Deep-Dive Analysis)

### أ. بيئة التشغيل والربط الديناميكي (Dynamic Linkage & WSL2)
- يعمل النظام المضيف على بيئة **WSL2**، حيث يقوم Docker بربط ملفات تعريف كرت الشاشة الأساسية (Driver API) بنجاح تام داخل الكونتينر، مما جعل ملف `/usr/lib/x86_64-linux-gnu/libcuda.so` متاحاً.
- **الغياب الأولي للمكتبات:** بيئة `python:3.11-slim` هي بيئة مصغرة تفتقر افتراضياً إلى مكتبات الـ CUDA Toolkit.

### ب. الحقن الديناميكي للمكتبات (Surgical Library Injection)
لتجاوز المشكلة دون عمل إعادة بناء (Rebuild) للكونتينر أو تغيير الصورة الأساسية، قمنا بسحب الحزم الرسمية المصغرة من NVIDIA وحقن ملفات الربط الديناميكي التالية مباشرةً في مسار النظام (`/usr/lib/x86_64-linux-gnu/`):
1. **`libcudart.so.11.0`** (CUDA Runtime API).
2. **`libcublas.so.11` و `libcublasLt.so.11`** (مكتبات تسريع العمليات الخطية والمصفوفات).
3. **`libcudnn.so.9` و `libcudnn.so.8`** (مكتبات الشبكات العصبية العميقة).

---

## 🛑 3. السبب الجذري الحاسم لعدم الحل (The Unresolved Blocker)
على الرغم من نجاح عملية الربط الديناميكي وتوفير كافة الملفات المطلوبة لكرت الشاشة، استمر الانهيار (Segfault) عند استدعاء `Predictor::Init`. 

**السبب الفني الفعلي:**
تعاني النسخة المترجمة مسبقاً (Pre-compiled Binary Wheel) من حزمة `paddlepaddle-gpu==2.6.2` المرفوعة على مستودعات PyPI من **عدم توافق ثنائي عميق (Binary Incompatibility)** مع مكتبة خيوط المعالجة القياسية لـ GLIBC المتوفرة في توزيعة Debian/Slim المصغرة عند محاولة بناء المتغيرات وتخصيص الذاكرة داخل كرت الشاشة (`CreateVariables`). 

هذا التعارض يمثل **عطلاً مصنعياً في التوافق الثنائي** لتلك النسخة المحددة مع بيئة الـ Slim، ولا يتبع لنقص الملفات بل لطريقة ترجمة نواتها البرمجية (C++ Compiler Flags) الخاصة بتلك الحزمة.

---

## 💡 4. التوصيات والحلول المستقبلية المتاحة (Future Roadmap)

بما أن قيود العمل الحالية تمنع عمل Rebuild للكونتينر أو مسح/تغيير الحزمة، تم تطبيق **عزل ديناميكي** يسمح للمستندات النصية بالعمل بكفاءة تامة عبر المحرك النصي الأصلي كما حدث في ملف `المؤشرات-الصحية.pdf`.

إذا تقرر لاحقاً حل مشكلة الـ OCR للصور المعقدة على الـ GPU، يوصى بتطبيق أحد الخيارين التاليين:

### الخيار الأول: التحديث الجذري لصورة الـ Docker الأساسية (أوصى به للإنتاج)
استبدال الصورة المصغرة بصورة تدعم بيئة CUDA والترجمة محلياً بشكل رسمي في `Dockerfile`:
```dockerfile
FROM nvidia/cuda:11.8.0-runtime-ubuntu22.04
# تثبيت بايثون والاعتماديات لاحقاً
```
هذا يضمن تطابق الرموز الثنائية (Symbols) بين نظام التشغيل ومحرك الاستنتاج.

### الخيار الثاني: استخدام المعالج العادي للصور (CPU Fallback)
إذا ظل الكونتينر كما هو، يمكن تثبيت حزمة `paddlepaddle` (نسخة الـ CPU العادية) جنباً إلى جنب أو كبديل للحزمة الحالية، حيث أثبتت التجارب أنها لا تعاني من هذا الانهيار الثنائي داخل بيئة الـ Slim وتعمل باستقرار تام.
